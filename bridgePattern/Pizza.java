package bridgePattern;

//Definisce l'interfaccia per vari tipi di pizze, stabilendo metodi come prepare, bake, e serve.
public interface Pizza {
    void prepare();
    void bake();
    void serve();
}
